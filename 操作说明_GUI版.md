# 钉钉邮件发送 GUI 操作说明

## 1. 工具定位

这个工具面向“本地批量发企业邮箱邮件”的日常场景。

- 一行 = 一封邮件任务
- 正文 = `开头/补充内容` + `Markdown 文件正文`
- 图片会自动转为 `CID` 内联图片
- 定时发送采用“托盘常驻”模式：关闭主窗口后，程序仍在托盘中运行

## 2. 使用前准备

请确认以下信息已经准备好：

- 发件账号：首次使用在界面中填写并保存
- SMTP 服务器：`smtp.qiye.aliyun.com`
- 端口：`465`
- 安全方式：`SSL`
- SMTP 授权码：阿里企业邮箱后台生成的客户端授权码
- 登录信息文件：保存在用户配置目录 `%LOCALAPPDATA%\DingMailSender\conn_profile.json`；旧版程序目录/工作目录中的配置会自动迁移
- Windows 下保存的 SMTP 授权码会通过 DPAPI 加密

## 3. 推荐操作流

1. 打开 EXE 或运行 `run_gui.ps1`
2. 在顶部输入发件邮箱与 SMTP 授权码，点击“连接并测试”（连接成功后会把登录信息加密写入 `conn_profile.json`，下次启动自动带出）
3. 点击“下载任务包模板”
4. 打开生成的 `tasks.xlsx`，按一行一封邮件的方式填写
5. 返回软件后点击“重新加载任务包”
6. 选中某一行，点击“预览当前邮件”
7. 确认无误后：
   - 保存草稿：点击“保存到草稿箱（选中）”，再去邮箱草稿箱微调后手工发送
   - 立即发送：点击“立即发送选中”
   - 定时发送：先在任务中勾选定时并填写时间，再点击“加入定时队列”

## 4. 任务包目录结构

```text
任务包目录/
  tasks.xlsx
  README_操作说明.md
  content/
    示例正文.md
  assets/
  attachments/
```

说明：

- `tasks.xlsx`：核心任务表
- `README_操作说明.md`：任务包内的简版填写说明
- `content/`：Markdown 正文
- `assets/`：Markdown 内引用的图片
- `attachments/`：邮件附件

## 5. tasks.xlsx 字段说明

- `任务ID`
  程序自动生成，通常无需手改；如果缺失或重复，重新加载或保存时会自动修复
- `是否启用`
  `是/否`
- `收件人`
  多个邮箱用分号分隔
- `抄送人`
  可为空；多个邮箱用分号分隔
- `主题`
  邮件主题
- `开头/补充内容`
  支持换行，可写例如 `**总：晚上好**`
- `Markdown路径`
  推荐写相对路径，例如 `content/示例正文.md`；只允许引用当前任务包目录内的文件
- `是否有附件`
  仅作辅助阅读，程序以“附件路径”是否存在为准
- `附件路径`
  多个附件用分号分隔，例如 `attachments/a.pdf; attachments/b.pdf`；不支持任务包外路径
- `是否定时发送`
  `是/否`
- `定时发送时间`
  建议格式：`2026-03-18 20:30:00`
- `备注`
  可选，用于区分任务

## 6. 软件内编辑

如果不想打开 Excel，也可以直接在软件中操作：

- `新增任务`
- `编辑任务`
- `复制任务`
- `删除任务`

说明：

- 双击任务表中的某一行，会打开轻量编辑器
- 编辑器中可以直接选择 Markdown 文件和附件
- 软件内修改会同步写回 `tasks.xlsx`
- 软件内修改只会更新 `Tasks` 工作表，会保留 `tasks.xlsx` 里的其他工作表
- 如果某些任务的 `任务ID` 缺失或重复，软件会在保存时自动修复
- 如果 Excel 正在占用 `tasks.xlsx`，保存会失败；请先关闭 Excel 再重试

## 7. 预览说明

点击“预览当前邮件”后，会弹出单封邮件预览窗。

预览窗中可以看到：

- 收件人 / 抄送人
- 主题
- Markdown 来源
- 附件列表
- 最终正文样式

如果当前任务有问题，例如：

- Markdown 路径不存在
- 图片路径不存在
- 附件路径不存在

预览窗会直接显示错误原因。

## 8. 定时发送说明

V1 的定时发送规则如下：

- 定时任务必须先“连接 SMTP”
- 定时任务必须手动点击“加入定时队列”
- 时间到后由本机程序自动发送
- 关闭主窗口时，程序会最小化到系统托盘继续等待
- 只有从托盘菜单点击“退出程序”，程序才会真正结束

如果程序已彻底退出，尚未到点的任务不会自动发送。

## 9. 状态说明

任务表中的常见状态：

- `可发送`
- `校验失败`
- `已加入定时队列`
- `发送中`
- `发送成功`
- `发送失败`
- `已停用`

建议处理方式：

- `校验失败`：先修正路径、邮箱格式或时间
- `发送失败`：看状态提示和输出目录日志，再用“重试失败项”

## 10. 输出目录

每次发送后，程序会在 `runs\` 目录下生成一次输出。

典型内容：

- `logs\send.log` 或 `logs\drafts.log`：运行日志
- `manifest.csv`：每封邮件的状态清单（收件人与错误信息已脱敏）
- `previews\`、`eml\`：默认为空；设置环境变量 `DINGMAIL_SAVE_DEBUG_ARTIFACTS=1` 后才会写入 HTML 预览与邮件原文

## 11. 常见问题

### 11.1 连接失败

优先检查：

- 是否填写了 SMTP 授权码，而不是网页登录密码
- 是否已开通 IMAP（保存草稿功能需要 `imap.qiye.aliyun.com:993`）
- 企业邮箱后台是否已开启 SMTP
- 服务器是否为 `smtp.qiye.aliyun.com`
- 端口是否为 `465`

### 11.2 保存失败

通常是 `tasks.xlsx` 正被 Excel 占用。

处理方式：

1. 关闭 Excel
2. 回到软件重新保存

### 11.3 定时任务没有发送

优先检查：

- 是否已点击“加入定时队列”
- 程序是否还在托盘中运行
- SMTP 是否仍然保持连接

## 12. 当前边界

当前版本有两个明确边界：

- 程序“彻底退出”后，不会继续定时发送
