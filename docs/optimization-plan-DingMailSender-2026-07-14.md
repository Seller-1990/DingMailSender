# DingMailSender 非安全类修复优化方案

日期：2026-07-14
范围：正确性、稳定性、性能与状态一致性
明确排除：凭据迁移、运行快照隐私、日志脱敏等安全类问题

## 1. 结论

本轮只处理能够直接降低发送失败、资源泄漏、内存峰值或状态错乱概率的问题。维护性重构采用渐进原则，不为追求结构整齐而改写稳定代码。

| 优先级 | 问题 | 判断 | 本轮动作 |
|---|---|---|---|
| P0 | 空 Markdown 与空引言可通过校验，执行时才报“邮件正文为空” | 必须修复 | 在任务校验阶段组合正文并拒绝空正文 |
| P0 | SMTP/IMAP `__enter__` 中途失败时已创建连接未可靠关闭 | 必须修复 | 初始化失败时主动关闭连接并保持原异常 |
| P1 | 附件与内联图片无限制读取到内存 | 必须优化 | 增加单文件和单邮件总载荷上限，发送前校验 |
| P1 | 编辑任务时先清空运行态，再保存 `tasks.xlsx` | 必须修复 | 文件保存成功后再提交运行态重置 |
| P2 | 投递结果使用分散的字符串状态协议 | 已完成 | 增加 `DeliveryStatus(StrEnum)`，统一业务层、运行态和历史统计 |
| P2 | GUI mixin 和部分文件偏大 | 已完成 | 按任务包、任务编辑、表格状态和对话框职责拆分，保留兼容入口 |
| P3 | 发布版本与 Release 治理不完整 | 已完成 | 建立单一版本来源、Tag 校验、版本化资产、GitHub Release 与回滚手册 |

## 2. 设计方案

### 2.1 空正文提前校验

- 复用真实发送使用的正文组合规则，而不是另写一套近似判断。
- 空 Markdown + 空引言：校验失败。
- 空 Markdown + 非空引言：校验通过。
- 保持预览和发送阶段的防御性异常，避免绕过校验的调用产生空邮件。

验收：`validate_task()` 在执行投递前返回“邮件正文为空”。

### 2.2 SMTP/IMAP 初始化失败清理

- SMTP 在 `ehlo`、`starttls`、二次 `ehlo` 或 `login` 任一步失败时调用 `close()`。
- IMAP 在 `login` 或草稿箱发现失败时优先 `logout()`，失败后再尝试 `shutdown()`。
- 清理异常不得覆盖原始连接或认证异常。

验收：模拟登录/初始化失败，断言底层连接清理方法被调用，调用方仍收到原异常。

### 2.3 邮件载荷限制

采用命名常量，限制原始文件载荷：

- 单个附件：15 MiB。
- 单张内联图片：5 MiB。
- 单封邮件全部附件与内联图片合计：18 MiB。

18 MiB 原始二进制经 MIME/Base64 编码后通常接近 24 MiB，可为常见 25 MiB 邮件限制保留协议开销。校验只使用文件元数据，不在发现超限前读取大文件内容。

验收：超限文件在 `validate_task()` 阶段失败；合法小文件保持原行为；总载荷超限也会失败。

### 2.4 保存与运行态一致性

- `_persist_tasks()` 增加“保存成功后需要重置的任务 ID”参数。
- 先原子写入 `tasks.xlsx`，成功后再替换内存任务并重置对应运行态。
- 保存失败时，现有任务列表、发送状态、错误信息和最近结果均保持不变。

验收：模拟 Excel 写入失败，编辑任务已有运行态不被清空。

## 3. 实施顺序

1. 先增加四组失败回归测试，确认能够复现现有问题。
2. 实施正文校验和载荷检查。
3. 实施 SMTP/IMAP 失败清理。
4. 调整 GUI 持久化提交顺序。
5. 运行定向测试、完整单元测试、`compileall` 和仓库卫生检查。
6. 增量更新代码图谱，复核影响范围。
7. 收尾投递状态类型化，并清理全仓 Ruff 既有问题。

## 4. 风险控制

- 不修改任务包格式、连接配置格式、运行目录格式或发送结果格式。
- 不引入新依赖。
- 不进行 GUI 大规模拆分。
- 载荷阈值集中放在 `constants.py`，后续可按实际邮箱服务能力调整。
- 真实 SMTP/IMAP smoke 仍由环境变量门控，本轮不向真实邮箱发送测试邮件。

## 5. 完成标准

- 新增回归测试覆盖四类问题。
- 全部现有测试通过。
- Python 编译检查通过。
- 代码图谱完成增量更新。
- 未覆盖或无法执行的验证项在最终结果中明确列出。

## 6. 实施结果

状态：已完成。

- 已实现空正文提前校验，并保留“仅引言正文”合法路径。
- 已实现 SMTP/IMAP 初始化失败时的连接清理。
- 已实现 15 MiB 附件、5 MiB 内联图片、18 MiB 总文件载荷限制，直接渲染路径同样会在读取大文件前拒绝。
- 已将任务运行态重置移动到 `tasks.xlsx` 保存成功之后；保存失败保持原内存任务与运行态。
- 已更新 README 中的载荷限制说明。
- 已增加 `DeliveryStatus(StrEnum)`，投递结果构造时会规范化合法状态并拒绝未知状态；GUI 统计不再依赖分散的字符串字面量。
- 运行历史遇到旧版或未来版本的未知状态时会显示“未识别状态”，不再静默漏算。
- 已修复 `task_package.py` 的 `E731`；测试目录因需要在未安装包时引导 `src/` 路径，对 `E402` 增加了说明性 Ruff 豁免。
- 单元测试：73 项执行，其中 71 项通过，2 项真实邮箱 smoke 因未配置环境变量而跳过。
- `compileall`、全仓 Ruff、仓库卫生检查、`git diff --check` 通过。
- 版本化 EXE 已完成 PyInstaller 构建、SHA256 审计、离屏启动 smoke、进程清理和重复覆盖构建验证。
- 代码图谱已完整重建为 52 个文件、540 个节点、4,542 条边，其中包含 73 个测试节点。

## 7. 后续治理实施结果

- `dialogs.py` 改为兼容导入门面，任务编辑器、邮件/文档预览、运行历史分别进入独立模块。
- `main_tasks.py` 改为兼容聚合层，任务包操作、任务增删改、表格与状态刷新分别进入独立 mixin。
- GUI 拆分不改变 `MainWindow` 的外部入口、任务模型、任务包格式或投递行为；相关 GUI 回归测试保持通过。
- `dingmail.__version__` 成为唯一版本来源，`pyproject.toml` 通过 setuptools 动态读取。
- 新增 Tag/版本一致性检查、版本化构建脚本、GitHub Release 工作流、SHA256 审计、启动 smoke、变更日志和发布/回滚手册。
- 本轮不创建或推送真实 Tag，也不触发远程 GitHub Release；正式发布仍需人工确认版本内容后执行。
